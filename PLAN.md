# プロジェクト改善計画

## 背景と目的
- 既存のLPが Create React App テンプレートの既定設定を残したまま運用されており、ブランディング・SEO・フォーム送信体験に課題がある。
- アニメーションやCTAの実装が断片化しており、コンポーネント構造・アクセシビリティ・保守性を高める必要がある。
- 将来の運用を見据え、最低限のテストおよび開発体験（DX）を整備する。

## 全体方針
1. **基盤整備**: メタ情報・設定ファイル・依存関係を現状に即して整理し、React 19 との齟齬や不要ライブラリを解消する。
2. **ルーティングの正規化**: ハッシュ遷移を `react-router-dom` に置き換え、URLと表示コンテンツの対応を明確化する。
3. **再利用性向上**: CTAやIntersectionObserver処理を共通化し、各セクションのデータを定義ファイルに集約する。
4. **フォーム改善**: `react-hook-form` + `zod` によるバリデーションと送信フローの安定化を図る。
5. **アクセシビリティ・計測**: 見出し階層、aria属性、画像最適化、Web Vitals 計測の導入で品質を底上げする。
6. **品質保証**: Jest/RTLによる基本的なUI挙動テストとLintの厳格化でリグレッションを抑止する。

## フェーズ詳細

### Phase 1: 基盤整備
- `public/index.html` の `<html lang>`、`<title>`、`meta description`、OGPタグの整備。
- `README.md` をLP向けの運用手順・ビルド手順へ刷新。
- `tsconfig.json` の `target`/`lib` を ES2020 相当に更新し、CRA残存設定を見直す。
- Stagewise Toolbar を開発時のみ動的読み込みするか、不要であれば削除。
- 依存バージョンの整合確認（React 19 に合わせ `@types/react` 等も整理）。

### Phase 2: ルーティング整理
- 依存追加無しで動作する軽量ルーター層を実装し、`/`, `/reserve`, `/thanks` の3ルートを作成。
- `App.tsx` をルート構成に合わせて再構築し、ハッシュ依存ロジックを除去。
- スクロール復帰・アニメーション開始をルート遷移に連動させるユーティリティを導入。

### Phase 3: コンポーネントとフォーム改善
- 共通CTAコンポーネントとデータ定義 (`src/constants/…`) を追加。
- IntersectionObserver を `useRevealOnScroll` フックへ抽象化し、各セクションで流用。
- `ReserveForm` を `react-hook-form` + `zod` で再実装し、Formspree送信後の遷移を `useEffect` で制御。
- フォームの希望日時やチェックボックスをフィールド配列／コントローラーで堅牢化。

### Phase 4: アクセシビリティと計測
- 見出し階層、ボタンの `aria-label`、エラー表示の `aria-live` を補強。
- 画像に `loading="lazy"` と `sizes` を付与、主要ビジュアルの `picture` 化を検討。
- `reportWebVitals` を実装し、計測結果をコンソール出力または分析基盤へ送信する仕組みを追加。

### Phase 5: テスト・仕上げ
- Formとルーティング、CTA挙動のJest + RTL テストを作成。
- `npm run lint` スクリプトを追加/更新し、CI向けに `--max-warnings=0` を設定。
- 変更点を README/TODO に反映し、デプロイ手順を最終確認。

## リスクと軽減策
- **依存更新によるビルドエラー**: 段階的に `npm install` を実行し、`package-lock.json` の差分を都度確認する。
- **フォーム仕様変更による差異**: クライアントと仕様擦り合わせを行い、項目名・データ形式の変更を明確化。
- **大規模差分によるレビュー負荷**: フェーズ毎にコミットを分割し、影響範囲を限定する。

## 成功指標
- Lighthouse パフォーマンス・アクセシビリティスコア 90 以上。
- Formspree 実環境での送信成功率 100%（テスト送信で確認）。
- `npm run build` とテストがクリーンに通ること。
