# レビュー所見

## High Priority
- **日本語コンテンツの文字化けが発生していた（対応済み）**  
  - 対象: `README.md`, `public/index.html`, `src/components/*` など広範囲。  
  - 影響: LP 全体の文章が判読できず、UX・SEO・ブランドイメージを著しく損なっていた。  
  - 対応: 主要ファイルを UTF-8 で再作成し、日本語コピーを復元。

- **ビルド成果物（`build/`, `.next/`）がリポジトリに含まれていた（対応済み）**  
  - 影響: 不要な差分が発生し、デプロイ成果物がそのまま公開リポジトリに残るセキュリティリスクがあった。  
  - 対応: `git rm --cached` で追跡解除し、`.gitignore` に `.next/` を追加。

## Medium Priority
- **レビューセクションの CTA が正しい遷移先を指していなかった（対応済み）**  
  - 影響: 「空き状況を相談する」リンクが機能せず、フォームへの導線が途切れていた。  
  - 対応: `CTAButton` を用いたルーティングに差し替え、`/reserve` へ確実に遷移するよう修正。

- **外部トラッキングスクリプトの運用ルールが不明（未対応）**  
  - 対象: `public/index.html` の `acsKeep` スニペット。  
  - リスク: 利用目的や同意取得フローがドキュメント化されておらず、プライバシー・パフォーマンス面の懸念が残る。  
  - 提案: 必要性と設置理由を整理し、同意取得後にロードする仕組みを検討。

## Low Priority
- **クイック日付ボタン押下時にエラー表示が残る問題（対応済み）**  
  - 影響: 推奨日付ボタンを選択してもバリデーションが再評価されず、利用者が混乱する恐れがあった。  
  - 対応: `setValue` に `shouldValidate: true` を追加し、即時にエラーが解消されるよう調整。
